build:
  docker:
    web: Dockerfile

# Optional, only if your project requires a specific stack
# stack: container
run:
  web: java $JAVA_OPTS -Dserver.port=$PORT -jar target/your-spring-boot-app.jar
